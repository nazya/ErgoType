#ifndef ERGOTYPE_HID_BPF_VMLINUX_H
#define ERGOTYPE_HID_BPF_VMLINUX_H

/* Linux generates vmlinux.h from BTF; the native firmware adapter uses the
 * corresponding compatibility types directly. */
#include "usb_host/hid_bpf_static.h"

#endif
