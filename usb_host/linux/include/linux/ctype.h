#ifndef USB_HOST_LINUX_CTYPE_H
#define USB_HOST_LINUX_CTYPE_H

/*
 * The firmware C library provides the character classification operations
 * reached by the pinned HID drivers.
 */
#include <ctype.h>

#endif
