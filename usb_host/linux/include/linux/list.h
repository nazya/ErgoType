#ifndef USB_HOST_LINUX_LIST_H
#define USB_HOST_LINUX_LIST_H

/*
 * Reduced firmware contract: hid_compat.h owns the Linux-shaped intrusive
 * list primitives used by linked HID/input code.
 */
#include "hid_compat.h"

#endif
